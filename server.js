const express=require("express"),path=require("path"),fs=require("fs");const app=express(),PORT=process.env.PORT||3000,productsPath=path.join(__dirname,"data","products.json");app.use(express.json());app.use(express.static(path.join(__dirname,"public")));app.get("/api/health",(req,res)=>res.json({ok:true,service:"catalog-api"}));app.get("/api/products",(req,res)=>{const products=JSON.parse(fs.readFileSync(productsPath,"utf8"));const search=String(req.query.search||"").toLowerCase(),category=String(req.query.category||"all").toLowerCase();const filtered=products.filter(p=>(!search||p.name.toLowerCase().includes(search)||p.description.toLowerCase().includes(search))&&(category==="all"||p.category.toLowerCase()===category));res.json({count:filtered.length,products:filtered})});app.get("*",(req,res)=>res.sendFile(path.join(__dirname,"public","index.html")));app.listen(PORT,()=>console.log(`NovaStore running on http://localhost:${PORT}`));